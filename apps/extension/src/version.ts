/**
 * The extension's build version, shown in the panel header (BL-014). Kept in
 * sync with `public/manifest.json`; the CDN/manifest update channel is BL-015.
 */
export const EXTENSION_VERSION = '0.0.0';
